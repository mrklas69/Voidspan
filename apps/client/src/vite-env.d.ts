/// <reference types="vite/client" />

// Vite define — inject z vite.config.ts. Regeneruje se při každém startu
// Vite procesu (nikoli při HMR). Viditelné v Top Bar identity tooltipu.
declare const __BUILD_ID__: string;
